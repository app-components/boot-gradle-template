server:
  port: 8080

spring:
  application:
    name: backend

  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:15432}/${DB_NAME:backend}
    username: backend
    password: password

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
